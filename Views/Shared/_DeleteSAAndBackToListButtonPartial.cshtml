@using LMS.Models.Assessment
@model Summative
<div class="row">
    <div class="col-6">
        <input type="submit" class="btn btn-danger form-control" asp-route-id="@Model.Id" value="Delete" />
    </div>
    <div class="col-6">
        <a asp-action="SummativeList" asp-route-id="@Model.CourseUnitId" class="btn btn-success form-control">Back to List</a>
    </div>
</div>

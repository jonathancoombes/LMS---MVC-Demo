@using LMS.Models.Assessment
@model Formative
<div class="row">
    <div class="col-6">
        <input type="submit" class="btn btn-danger form-control" asp-route-id="@Model.Id" value="Delete" />
    </div>
    <div class="col-6">
        <a asp-action="FormativeList" asp-route-id="@Model.CourseTopicId" class="btn btn-success form-control">Back to List</a>
    </div>
</div>
